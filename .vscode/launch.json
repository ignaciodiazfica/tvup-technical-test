{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Debuggear App",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}/src"
      }
    ]
}